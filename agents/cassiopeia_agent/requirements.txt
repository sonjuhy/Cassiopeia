# Cassiopeia Agent Dependencies

# FastAPI & ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Pydantic v2
pydantic>=2.7.0

# Environment variables
python-dotenv>=1.0.1

# Redis (async)
redis[hiredis]>=5.0.0

# Agent messaging SDK
cassiopeia-sdk>=0.3.0

# Google Gemini (Primary NLU)
google-genai>=1.0.0

# Anthropic Claude (Fallback NLU)
anthropic>=0.40.0

# Databases (Persistent session history)
asyncpg>=0.30.0    # PostgreSQL
aiomysql>=0.2.0    # MySQL
aiosqlite>=0.20.0  # SQLite

# HTTP client for dispatching tasks to sub-agents
httpx>=0.27.0

# Docker SDK (SandboxTool - DockerSandbox 내부 실행)
docker>=7.1.0

# System monitoring
psutil>=6.0.0

# Timezone support
tzdata>=2024.1
